apiVersion: v1
kind: Service

metadata:
  name: college-event-service

spec:
  type: NodePort

  selector:
    app: college-event

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30007